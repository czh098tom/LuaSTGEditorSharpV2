<Window x:Class="LuaSTGEditorSharpV2.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LuaSTGEditorSharpV2"
        mc:Ignorable="d"
        Title="Input" 
        MinWidth="350" 
        MinHeight="110"
        Width="350"
        SizeToContent="Height" 
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        FocusManager.FocusedElement="{Binding ElementName=contentTextBox}">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="10" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition />

        </Grid.RowDefinitions>

        <!--Find Controls-->
        <Label Grid.Column="1" Grid.Row="1">_Content:</Label>
        <TextBox Name="contentTextBox" Text="{Binding Text}" Grid.Column="2" Grid.Row="1" />

        <!--Action Controls-->
        <StackPanel Grid.Column="4" Grid.Row="1" Grid.RowSpan="6">
            <Button Name="okButton" Click="OKButton_Click" Height="25" Width="70" IsDefault="True" Margin="0,0,0,5">OK</Button>
            <Button Name="closeButton" Click="CloseButton_Click" Height="25" Width="70" IsCancel="True">Close</Button>
        </StackPanel>

    </Grid>
</Window>
