<UserControl x:Class="LuaSTGEditorSharpV2.Package.LinqSTG.Windows.View.Editor.IntervalTypeEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="40" d:DesignWidth="100">
    <UserControl.Resources>
        <ControlTemplate x:Key="IntervalCircleToggle" TargetType="ToggleButton">
            <Grid Width="14" Height="14">
                <Ellipse x:Name="Circle"
                         Width="12" Height="12"
                         StrokeThickness="1.5"
                         Stroke="#FF3A3A3A"
                         Fill="#22FFFFFF"
                         VerticalAlignment="Center"
                         HorizontalAlignment="Center"/>
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="Circle" Property="Stroke" Value="#FFF8F8F8"/>
                    <Setter TargetName="Circle" Property="Fill" Value="#FFF0F0F0"/>
                </Trigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsMouseOver" Value="True"/>
                        <Condition Property="IsChecked" Value="False"/>
                    </MultiTrigger.Conditions>
                    <Setter TargetName="Circle" Property="Stroke" Value="#FF7C7C7C"/>
                </MultiTrigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style x:Key="HeadConnectorStyle" TargetType="Rectangle">
            <Setter Property="Fill" Value="#FF3A3A3A"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsChecked, ElementName=HeadToggle}" Value="True">
                    <Setter Property="Fill" Value="#FFF0F0F0"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="TailConnectorStyle" TargetType="Rectangle">
            <Setter Property="Fill" Value="#FF3A3A3A"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsChecked, ElementName=TailToggle}" Value="True">
                    <Setter Property="Fill" Value="#FFF0F0F0"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
        <ToggleButton x:Name="HeadToggle"
                      Template="{StaticResource IntervalCircleToggle}"
                      ToolTip="Head closed"
                      IsChecked="{Binding HeadClosed, Mode=TwoWay}"/>
        <Rectangle Width="5" Height="1.5"
                   Style="{StaticResource HeadConnectorStyle}"
                   VerticalAlignment="Center"/>
        <Rectangle Width="5" Height="5" Fill="#FFF0F0F0" VerticalAlignment="Center"/>
        <Rectangle Width="5" Height="1.5" Fill="#FFF0F0F0" VerticalAlignment="Center"/>
        <Rectangle Width="5" Height="5" Fill="#FFF0F0F0" VerticalAlignment="Center"/>
        <Rectangle Width="5" Height="1.5"
                   Style="{StaticResource TailConnectorStyle}"
                   VerticalAlignment="Center"/>
        <ToggleButton x:Name="TailToggle"
                      Template="{StaticResource IntervalCircleToggle}"
                      ToolTip="Tail closed"
                      IsChecked="{Binding TailClosed, Mode=TwoWay}"/>
    </StackPanel>
</UserControl>
